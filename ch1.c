//INLCUDE statements, used to use functions not in compiler
#include <stdio.h> 
#include <stdlib.h>
int main (void) {   //main functions --> where program is starts
    puts("Hello World"); //print text to screen

    //Another way to print to terminal
    printf("This is another way to print %s and numbers like: %d\n", "strings", 51);
    return EXIT_SUCCESS; //macro, lets user know everything worked
}
